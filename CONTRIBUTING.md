# 贡献

请先运行发布校验和最小续训验证。提交问题时提供依赖版本、GPU、完整命令、实际检查点名、随机种子和失败阶段；不要上传账号、SSH 配置或带私人路径的日志。

修改奖励或模型时保留旧实验对照，说明观测/动作/物理参数是否变化，并报告静止与运动技能是否退化。不要手动覆盖旧 run.json 来绕过兼容检查。实验输出放 artifacts/，提交文档中的聚合结果和必要的脱敏报告即可。

新增模型或素材请同时提供来源和授权信息。代码修改请注明适用范围，不把未经验证的能力写入 README。

发布清单 `SHA256SUMS.json` 覆盖当前发布文件；修改已列入清单的文件后需要同步更新相应摘要。CI 仅检查文件完整性、Python 语法和检查点兼容契约，不代替 GPU 训练验收。
